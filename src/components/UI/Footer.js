import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <div className={classes.footer}>
      <div className={classes.copy}>&copy; Nuno Rigo Fadilah.</div>
    </div>
  );
};

export default Footer;
